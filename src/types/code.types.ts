// eslint-disable-next-line no-shadow
enum StatusCode {
  ERROR_DATA = 400,
  ERROR_AUTH = 401,
  ERROR_FORBIDDEN = 403,
  ERROR_NOT_FOUND = 404,
  ERROR_CONFLICT = 409,
  ERROR_DEFAULT = 500,
}

export default StatusCode;
